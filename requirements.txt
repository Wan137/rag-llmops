# Python 3.11+

# --- Web Framework ---
django==5.0.6
djangorestframework==3.15.2
django-cors-headers==4.4.0
gunicorn==22.0.0

# --- LLM & RAG orchestration ---
langchain==1.3.11
# don't bump this past 0.3.31 - ragas 0.4.3 still imports langchain_community.chat_models.vertexai,
# which got removed in 0.4.x, and everything breaks on import. found the hard way.
langchain-community==0.3.31
langchain-chroma==1.1.0
langchain-google-genai==4.2.6

# --- Vector Database ---
chromadb==1.5.9

# --- Document Loaders ---
pypdf==4.3.1
python-docx==1.1.2
unstructured==0.15.0

# --- Embeddings (local, free, no API key) ---
langchain-huggingface==1.2.2
sentence-transformers==3.0.1
torch==2.3.1

# --- Config & Validation ---
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.0.1
pyyaml==6.0.2

# --- Evaluation / MLOps ---
setuptools==80.10.2  # newer setuptools dropped pkg_resources and mlflow 2.15 still needs it
mlflow==2.15.0
ragas==0.4.3

# --- Testing ---
pytest==8.3.2
pytest-django==4.8.0
pytest-asyncio==0.23.8

# --- Code Quality (dev-only) ---
ruff==0.5.7
mypy==1.11.1
black==24.8.0
